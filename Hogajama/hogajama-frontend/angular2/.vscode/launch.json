{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Open Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceRoot}/dist/",
            "userDataDir": "${workspaceRoot}/.chrome-debug"
        }
    ]
}
