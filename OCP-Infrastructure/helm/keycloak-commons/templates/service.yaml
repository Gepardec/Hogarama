apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak-commons.fullname" . }}
  labels:
    {{- include "keycloak-commons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloak-commons.selectorLabels" . | nindent 4 }}
