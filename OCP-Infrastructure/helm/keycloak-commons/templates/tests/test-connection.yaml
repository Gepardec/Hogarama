apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keycloak-commons.fullname" . }}-test-connection"
  labels:
    {{- include "keycloak-commons.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keycloak-commons.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
