---
  raspberries:
    hosts:
      192.168.100.141
    vars:
      sensors: '[{"name": "Pflanze","type": "wasser","channel": 0,"pin": 21}]'
      actors: '[{"name": "Pflanze","type": "GPIO","pin":21}]'
      ID_STR: "raspi-flexl"

  all:
    vars:
      BROKER_URLS: "[\"broker-amq-mqtt-ssl-57-hogarama.cloud.itandtel.at\"]"

    # Global vars:
      ansible_ssh_user: pi
      ansible_ssh_pass: raspberry

    # Network vars:
      SSID: "Gepardec"
      PSK: "AnsoMenOrkSmulUE"
      ip_router: 192.168.100.1

    # SETUP FLAGS
      update_raspberry: false
      setup_ssh: false
      setup_network: false

    # SSH Vars:
      ssh_keys:
        https://github.com/fhochleitner.keys
        https://github.com/ludwigsteindl.keys
...