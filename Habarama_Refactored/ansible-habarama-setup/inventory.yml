---
  raspberries:
    hosts:
      192.168.100.141
    vars:
      sensors: '[{"name": "Pflanze","type": "wasser","channel": 0,"pin": 21}]'
      actors: '[{"name": "Pflanze","type": "GPIO","pin":21}]'
      ID_STR: "raspi-flexl"
      ip_address: 192.168.100.141
      SENSOR_LOCATION: "Wiener Wildtierpark"

  all:
    vars:
      BROKER_URLS: "[\"broker-amq-mqtt-ssl-57-hogarama.cloud.itandtel.at\"]"
      client_python_dir: "{{ansible_env.HOME}}/client_python/"

    # Global vars:
      ansible_ssh_user: pi
      ansible_ssh_pass: raspberry

    # Network vars:
      SSID: "SSID"
      PSK: "Wifi Password"
      ip_router: 192.168.100.1

    # SETUP FLAGS
      update_raspberry: false
      setup_ssh: true
      setup_network: true

    # SSH Vars:
      ssh_keys:
        https://github.com/fhochleitner.keys
        https://github.com/ludwigsteindl.keys
...